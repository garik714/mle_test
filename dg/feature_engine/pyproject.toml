[project]
name = "feature_engine"
version = "0.1.0"
description = ""
authors = [
    {name = "Aleksandr Motuzov",email = "Aleksandr.Motuzov@ameriabank.am"}
]
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
]

[tool.poetry]
packages = [{include = "feature_engine", from = "src"}]

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
